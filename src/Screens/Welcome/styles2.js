import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        width: '96%',
        marginHorizontal: '2%'
    },
    input: {
        borderWidth: 1,
        borderColor: '#CECECE',
        borderRadius: 5
    },
    greeting: {
        textAlign: 'center',
        marginTop: 50
    }
})

export default styles;